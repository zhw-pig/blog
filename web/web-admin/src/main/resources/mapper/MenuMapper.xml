<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhw.blog.web.admin.mapper.MenuMapper">
    <select id="selectPermissionByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.permission
        FROM
             t_sys_user_role ur
             LEFT JOIN t_sys_role r ON ur.role_id = r.id
             LEFT JOIN t_sys_role_menu rm ON ur.role_id = rm.role_id
             LEFT JOIN t_sys_menu m ON m.id = rm.menu_id
        WHERE
              user_id = #{userId}
              AND r.state = 1
              AND m.state = 1
    </select>
</mapper>